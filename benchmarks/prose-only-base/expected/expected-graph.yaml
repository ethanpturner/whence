# Edges that genuinely exist and should be recovered. This list is deliberately short: almost
# nothing about this model's lineage is assertable from its metadata, and the scenario exists to
# check that the tool says so rather than filling the gap.

root:
  ref: { host: huggingface.co, namespace: mistralai, name: Mistral-7B-Instruct-v0.2 }
  revision: 63a8b081895390a26e140280378bc85ec8bce07a
  pinned: true

edges:
  - source: mistralai/Mistral-7B-Instruct-v0.2
    target: transformers
    relation: requires-package
    provenance: asserted-by-config
    verdict: unverifiable
    evidence_locator: "cardData.library_name"
    node_kind: package
