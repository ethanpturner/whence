# The declared base is unreachable and its namespace is free. Deletion does not contradict the
# lineage -- the derivation may well have happened and the base was removed afterwards -- so the
# verdict is `unverifiable` and the finding lives on the node.

root:
  ref: { host: huggingface.co, namespace: bartowski, name: AvelonLabs_OpenClaude-1.7B-Merged-GGUF }
  revision: 04a6e75d9df2f9e426b267cae641c0f58d7c6d6d
  pinned: true

edges:
  - source: bartowski/AvelonLabs_OpenClaude-1.7B-Merged-GGUF
    target: AvelonLabs/OpenClaude-1.7B-Merged
    relation: quantized-from
    provenance: asserted-by-card
    verdict: unverifiable
    evidence_locator: "cardData.base_model"
    notes: >-
      `asserted-by-card`, not `unresolvable`: the card plainly declares this base and the reference
      is well-formed. The target's unreachability is a property of the node (DEC-018).

  - source: bartowski/AvelonLabs_OpenClaude-1.7B-Merged-GGUF
    target: angrygiraffe/claude-opus-4.6-4.7-reasoning-8.7k
    relation: trained-on
    provenance: asserted-by-card
    verdict: unverifiable
    evidence_locator: "cardData.datasets[0]"
    node_kind: dataset

  - source: bartowski/AvelonLabs_OpenClaude-1.7B-Merged-GGUF
    target: lordx64/reasoning-distill-claude-opus-4-7-max
    relation: trained-on
    provenance: asserted-by-card
    verdict: unverifiable
    evidence_locator: "cardData.datasets[1]"
    node_kind: dataset

nodes:
  - ref: AvelonLabs/OpenClaude-1.7B-Merged
    pinned: false
    reachable: false
    properties:
      whence:namespace-state: free
      whence:risk: reregistrable-reference
    notes: >-
      Neither an organization nor a user holds `AvelonLabs`. The name is available for anyone to
      register, and a pipeline resolving this dependency by name would fetch whatever later
      occupies it (DEC-002).
